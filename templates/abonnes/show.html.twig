{% extends 'base.html.twig' %}

{% block title %}Abonnes{% endblock %}

{% block body %}
    <h1>Abonnes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ abonne.id }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ abonne.firstname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ abonne.lastname }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ abonne.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('abonnes_index') }}">back to list</a>

    <a href="{{ path('abonnes_edit', {'id': abonne.id}) }}">edit</a>

    {{ include('abonnes/_delete_form.html.twig') }}
{% endblock %}
