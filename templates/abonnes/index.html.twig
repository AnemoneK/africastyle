{% extends 'base.html.twig' %}

{% block title %}Abonnes index{% endblock %}

{% block body %}
    <h1>Abonnes index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Firstname</th>
                <th>Lastname</th>
                <th>Email</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for abonne in abonnes %}
            <tr>
                <td>{{ abonne.id }}</td>
                <td>{{ abonne.firstname }}</td>
                <td>{{ abonne.lastname }}</td>
                <td>{{ abonne.email }}</td>
                <td>
                    <a href="{{ path('abonnes_show', {'id': abonne.id}) }}">show</a>
                    <a href="{{ path('abonnes_edit', {'id': abonne.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('abonnes_new') }}">Create new</a>
{% endblock %}
